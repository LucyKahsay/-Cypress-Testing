Through this assignment, I learned how to cleanly organize automation workflows using standard test structures (describe, it, and hooks) and how to write stable locators using custom data attributes instead of brittle, long CSS selectors. I mastered the difference between automatic retry assertions like should() and direct data validations using expect(), while recognizing that hardcoded pauses like cy.wait(5000) should be avoided in favor of dynamic element waiting. Finally, I gained critical technical problem-solving skills by overcoming Linux network timeouts, learning how to bypass heavy application binary downloads using the CYPRESS_INSTALL_BINARY=0 flag to successfully execute headless automated tests directly through the system's native Chrome browser engine
